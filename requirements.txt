# Nixpacks/Railway install from this file and skip `pip install .` when it lists
# third-party pins only, so the src-layout package never becomes importable.
# Installing the project here pulls dependencies from pyproject.toml.
.
